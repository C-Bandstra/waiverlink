module.exports = {
  printWidth: 100,                    // Wider lines for more readable code
  tabWidth: 2,                        // 2 spaces per indentation level
  useTabs: false,                     // Use spaces instead of tabs
  semi: true,                         // End statements with semicolons
  singleQuote: true,                  // Prefer single quotes in JS/TS
  jsxSingleQuote: false,              // JSX still uses double quotes
  trailingComma: 'all',               // Commas after last items in objects/arrays
  bracketSpacing: true,               // Adds space inside object brackets
  arrowParens: 'always',              // Always include parens around arrow function params
  proseWrap: 'preserve',              // Don’t auto-wrap markdown text
  endOfLine: 'lf',                    // Line feed only (\n), good cross-platform setting
  embeddedLanguageFormatting: 'auto', // Format embedded languages (e.g., CSS in JS)
};
